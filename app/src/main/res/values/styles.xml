<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="auth_inputs_style">
        <item name="android:background">@drawable/bg_solid_white</item>
        <item name="android:ellipsize">end</item>
        <item name="android:lines">1</item>
        <item name="android:maxLines">1</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textColor">@color/manatee</item>
        <item name="android:textColorHint">@color/manatee</item>
        <item name="android:textSize">14.0sp</item>
    </style>

    <style name="active_button">
        <item name="android:layout_height">38.0dp</item>
        <item name="android:background">@drawable/bg_solid_red</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14.0sp</item>
    </style>

</resources>
